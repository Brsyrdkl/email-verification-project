spring:
  application:
    name: verification-service

  datasource:
    username: root
    password: 1598753
    url: jdbc:mysql://localhost:3308/verificationdb?createDatabaseIfNotExist=true
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update

  mail:
    properties:
      mail:
        smtp:
          starttls:
            enable: 'true'
          auth: 'true'
    host: smtp.gmail.com
    username: barisyurdakul.by@gmail.com
    password: 18083543574
    port: '587'

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    instance-id: ${spring.application.name}:${random.value}}
    prefer-ip-address: true
server:
  port: 0


