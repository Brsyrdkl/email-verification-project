spring:
  application:
    name: email-service

  datasource:
    username: root
    password: 1598753
    url: jdbc:postgresql://localhost:5432/emaildb?createDatabaseIfNotExist=true
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    instance-id: ${spring.application.name}:${random.value}}
    prefer-ip-address: true
server:
  port: 0


